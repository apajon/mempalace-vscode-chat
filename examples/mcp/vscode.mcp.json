{
  "_note": "This is a template. setup.sh generates the real .mcp.json automatically, pointing --directory at the canonical bridge path $HOME/.local/share/mempalace-mcp-bridge (a symlink created by setup.sh).",
  "mcpServers": {
    "mempalace": {
      "type": "stdio",
      "command": "/ABSOLUTE/PATH/TO/uv",
      "args": ["run", "--directory", "$HOME/.local/share/mempalace-mcp-bridge", "python", "scripts/run_mcp_server.py"]
    }
  }
}
